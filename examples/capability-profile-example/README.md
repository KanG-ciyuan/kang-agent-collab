# Capability Profile example

This example shows how to describe a runtime without turning capability into permission. Replace the hypothetical values with observations from a concrete, dated configuration.

`unknown` is better than an unsupported positive claim.
