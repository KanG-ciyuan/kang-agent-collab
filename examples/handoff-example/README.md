# Handoff example

This example describes a completed clean-state task. Replace every placeholder with evidence observed from the actual repository.

A receiving agent must not trust the file blindly. It performs the matching Takeover Check and re-verifies the authoritative entry, Manifest, branch, full HEAD, and working tree.
